****Create Triger*****

DELETE FROM product WHERE manufacturer_id =old.id

CREATE PROCEDURE `add_manufacturer`(IN `m_name` VARCHAR(50), IN `m_address` VARCHAR(100), IN `m_contact` VARCHAR(20)) NOT DETERMINISTIC CONTAINS SQL SQL SECURITY DEFINER INSERT INTO manufacturer VALUES (NULL,m_name,m_address,m_contact)



User
INSERT INTO `users` (`id`, `name`, `password`) VALUES (NULL, 'alo', 'abcd123'), (NULL, 'alauddin', 'abcd123');


*****Students*****
Tiggir*****
CREATE TRIGGER `delete_result` AFTER DELETE ON `students` FOR EACH ROW DELETE FROM results WHERE rstudent_id=OLD.stid

CREATE PROCEDURE `add_students`(IN `st_name` VARCHAR(50), IN `st_address` VARCHAR(100), IN `st_mobile` VARCHAR(20)) NOT DETERMINISTIC CONTAINS SQL SQL SECURITY DEFINER INSERT INTO students VALUES (NULL,st_name,st_address,st_mobile)